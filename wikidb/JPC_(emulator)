2.00	JPC_(emulator)	JPC (emulator)	JPC is an x86 emulator written in pure Java. It can run on any platform that supports the Java Virtual Machine. It creates a virtual PC compatible machine that can run MS-DOS and other x86 operating systems. Programs inside JPC can run up to 20% of the native processor speed. JPC was written by the Oxford University Subdepartment of Particle Physics.	
